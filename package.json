{
  "name": "animal-adoption-platform",
  "version": "1.0.0",
  "description": "A platform to connect shelters, associations, and individuals for pet adoption.",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha tests/**/*.test.js",
    "lint": "eslint src/**/*.js"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "^9.0.0",
    "eslint": "^7.0.0"
  }
}
